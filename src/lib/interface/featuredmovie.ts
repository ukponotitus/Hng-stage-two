import { StaticImageData } from "next/image";
export interface IFeaturedMovie {
    img: string;
    date: string;
    title: string;
    // imdp:string,
    // rate:string,
    // apple:string,
    // percent:string,
    description:string,
    id: number;
  }